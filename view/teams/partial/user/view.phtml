
<div class="property">
    <h4>Teams</h4>
    <?
        foreach ($this->team_users as $team_user):
            ?>
    <div class="values">
        <?
            echo sprintf('%s (%s)<br>', $team_user->getTeam()->getName(), $team_user->getRole()->getName());
            ?>
    </div>
    <?
        endforeach;
        ?>

</div>

